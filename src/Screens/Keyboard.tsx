import React from "react";
import { Text, View } from "react-native";

const Keyboard = () => {
    return (
        <View style={{ flex: 1 }}>
            <Text>Keyboard Screen</Text>
        </View>
    );
}

export default Keyboard;